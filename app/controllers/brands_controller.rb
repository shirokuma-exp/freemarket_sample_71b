class BrandsController < ApplicationController
  # def new
  #   @brand = Brand.new
  # end
  #   @brand = Brand.new(brand_params)
  # def

  # private
  # def item_params
  #   params.require(brand).permit(:name).merge(user_id: current_user.id)
  # end

end
